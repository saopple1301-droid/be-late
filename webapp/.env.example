# LIFF app ID from the LINE Developers console (create a LIFF app inside
# the same channel the bot uses, endpoint URL = where this PWA is hosted).
VITE_LIFF_ID=

# Base URL of the FastAPI backend (see ../.env's PUBLIC_BASE_URL).
VITE_API_BASE_URL=http://localhost:8000
